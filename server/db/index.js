const {
  firebase,
  auth,
  provider,
  firestore,
  realtimeDB,
  signInWithGoogle
} = require('./firebase')

module.exports = {
  firebase,
  auth,
  firestore,
  realtimeDB,
  provider,
  signInWithGoogle
}
